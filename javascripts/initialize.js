//--------------
// Initializers
//--------------   

app.stackView = new app.StackView(); 
app.callbackView = new app.CallbackView(); 
app.reminderView = new app.ReminderView(); 
app.allThreadsView = new app.AllThreadsView(); 
